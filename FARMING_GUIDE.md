# 🌾 ФУНКЦИОНАЛ ОПТИМИЗАЦИИ ФАРМА

## Описание

Новая система **автоматического расчета оптимального маршрута фарма** для Dota 2!

- ✅ Анализирует все доступные споты фарма
- ✅ Рассчитывает оптимальный маршрут
- ✅ Дает советы по маршруту (голосом)
- ✅ Учитывает опасность на карте
- ✅ Визуализирует маршрут стрелками (оверлей на экран)

## Схема работы

```
GameAnalyzer
    ↓ (позиция героя, враги)
FarmingOptimizer
    ↓ (рассчитывает маршрут)
OverlayRenderer
    ↓ (рисует стрелки на экран)
```

## Модули

### 1. `farming_optimizer.py`
**Рассчитывает оптимальный маршрут фарма**

Функционал:
- Список всех известных спотов фарма в Dota 2
- Расчет эффективности каждого спота
- Оптимизация маршрута (похоже на задачу коммивояжера)
- Учет опасности (враги рядом = спот дороже)
- Рекомендации по следующему споту

Примеры спотов:
- Линии (крипы): 8 GPM, низкая опасность
- Кампы джунгля (нейтралы): 10 GPM, средняя опасность
- Древние: 15 GPM, средняя опасность
- Стакованные кампы: 25 GPM, высокая опасность
- Рошан: 50 GPM, очень высокая опасность

### 2. `overlay_renderer.py`
**Рисует маршрут на экран (оверлей)**

Функционал:
- Захватывает экран через MSS
- Рисует стрелки между спотами
- Показывает позицию героя
- Цветовая кодировка приоритета
- Информационная панель
- Работает без вмешательства в файлы игры!

Визуализация:
```
🟢 Герой (зелёная точка)
  ↓ (стрелка)
🟡 Спот 1 (жёлтый - приоритетный)
  ↓
🟠 Спот 2 (оранжевый)
  ↓
🟣 Спот 3 (фиолетовый)
```

## Использование

### В коде

```python
# Инициализировать оптимизатор
optimizer = FarmingOptimizer()

# Рассчитать маршрут
farm_route = optimizer.calculate_farm_route(
    hero_position=(420, 650),
    team_danger_level=0.5
)

# Получить рекомендацию
next_spot = optimizer.get_next_spot()
print(f"Идите в {next_spot['spot_name']}")

# Запустить оверлей
renderer = OverlayRenderer()
renderer.set_farm_route(farm_route, hero_position)
renderer.start()
```

### В программе

1. **Автоматическая рекомендация:**
   - Каждые 15 сек система анализирует фарм
   - Озвучивает рекомендацию ("Направляйся в древние")
   - Обновляет маршрут в реальном времени

2. **Ручной запрос:**
   - Скажи "фарм" или "помощь фарм"
   - Тренер дает совет по оптимальному маршруту

## Координатная система

Используется **миникарта Dota 2** как система координат:
- Левый нижний угол: (0, 0)
- Правый верхний угол: (1024, 1024)
- Центр: (512, 512)

При отрисовке оверлея координаты масштабируются на размер экрана.

## Параметры FarmSpot

```python
@dataclass
class FarmSpot:
    name: str                    # "Древние (красные)"
    position: (x, y)             # Координаты на миникарте
    gold_per_minute: float       # Доход золота
    difficulty: float            # 0-1, опасность
    farm_type: FarmingType       # LANE, JUNGLE, STACK, ROSHAN
    distance_to_base: float      # Расстояние для отступления
    time_to_clear: float         # Время очистки в сек
```

## Функции оптимизации

### calculate_farm_route()
```python
def calculate_farm_route(
    hero_position: (x, y),
    inventory_space: int = 6,
    team_danger_level: float = 0.5
) -> List[FarmSpot]
```

- Фильтрует безопасные споты на основе опасности
- Сортирует по эффективности (GPM/расстояние)
- Возвращает оптимальный маршрут

### get_next_spot()
```python
def get_next_spot() -> Dict:
    return {
        "status": "success",
        "spot_name": "Древние (красные)",
        "position": (600, 650),
        "distance": 150,
        "gold_per_minute": 15,
        "recommendation": "Направляйся в Древние"
    }
```

## Визуализация оверлея

### Компоненты
1. **Героя** - большой зелёный кружок
2. **Стрелки** - направление между спотами
3. **Номера** - порядок посещения
4. **Информационная панель** - текущий маршрут

### Цвета
- 🟢 Зелёный (спот 1, приоритет)
- 🟡 Жёлтый (спот 2)
- 🟠 Оранжевый (спот 3)
- 🟣 Фиолетовый (остальные)

## Как это работает в реальном времени

```
Каждые 15 сек:
1. GameAnalyzer получает позицию героя
2. Определяет врагов на карте
3. Рассчитывает уровень опасности
4. FarmingOptimizer считает маршрут
5. OverlayRenderer рисует на экран
6. Coach озвучивает совет ("Идите в Древние")
```

## Параметры конфигурации

В `coach.py`:
```python
self.enable_farming_tips = True      # Включить советы по фарму
self.farm_analysis_cooldown = 15     # Интервал анализа (сек)
```

## Ограничения текущей версии

- ⚠️ Координаты спотов фиксированные (примерные)
- ⚠️ При полной интеграции нужна Dota 2 API
- ⚠️ Оверлей экспериментальный (нужен платформозависимый код)

## Будущие улучшения

- [ ] Интеграция с Dota 2 API для точных координат
- [ ] Машинное обучение для предсказания врагов
- [ ] Анализ логов игры для адаптивного маршрута
- [ ] Визуализация тепловой карты опасности
- [ ] Стрелки в игре (через консоль gmod)

## Тестирование

```bash
python test_coaching.py
```

Проверит:
- ✓ Инициализацию оптимизатора
- ✓ Расчет маршрута
- ✓ Рекомендации
- ✓ Отрендеринг оверлея

## Интеграция

Включен в `coach.py`:
- Автоматический анализ фарма каждые 15 сек
- Голосовые рекомендации
- Визуализация маршрута

---

**Наслаждайся оптимальным фармом!** 🌾
