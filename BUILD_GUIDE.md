# 📦 СБОРКА EXE ДЛЯ DOTA COACH

Руководство по созданию исполняемого файла для Windows/Linux.

## 🖥️ WINDOWS (Самый простой способ)

### Вариант 1: Автоматическая сборка (рекомендуется)

1. **Установи Python** (если ещё не установлен)
   - Скачай с https://www.python.org/
   - ✅ Отметь "Add Python to PATH" при установке!

2. **Двойной клик на `build_windows.bat`**
   ```
   build_windows.bat ← Просто кликни!
   ```

3. **Дождись завершения сборки**
   - Скрипт сам установит зависимости
   - Скрипт сам запустит PyInstaller
   - Автоматически откроет папку `dist`

4. **Используй `dist/DotaCoach.exe`**
   - Копируй вместе с `.env` файлом
   - И можешь распространять!

### Вариант 2: Ручная сборка

Если автоматический скрипт не работает:

```bash
# 1. Установи зависимости
pip install -r requirements.txt

# 2. Собери exe
pyinstaller --onefile ^
    --name "DotaCoach" ^
    --add-data ".env.example;." ^
    --hidden-import=speech_recognition ^
    --hidden-import=pyttsx3 ^
    --hidden-import=requests ^
    --hidden-import=psutil ^
    --hidden-import=dotenv ^
    main.py

# 3. Готово! Файл в папке dist\
```

---

## 🐧 LINUX / MAC

### Вариант 1: Автоматическая сборка

```bash
# 1. Дай права на исполнение
chmod +x build_linux.sh

# 2. Запусти скрипт
./build_linux.sh

# 3. Результат в dist/DotaCoach
```

### Вариант 2: Ручная сборка

```bash
# 1. Создай виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 2. Установи зависимости
pip install -r requirements.txt

# 3. Собери
pyinstaller --onefile \
    --name "DotaCoach" \
    --add-data ".env.example:." \
    --hidden-import=speech_recognition \
    --hidden-import=pyttsx3 \
    --hidden-import=requests \
    --hidden-import=psutil \
    --hidden-import=dotenv \
    main.py

# 4. Готово!
./dist/DotaCoach
```

---

## 📝 ПОСЛЕ СБОРКИ

### Структура папки `dist/`

```
dist/
├── DotaCoach.exe          ← Основной файл
├── .env                   ← Конфиг (НУЖНО скопировать!)
├── _internal/             ← Вспомогательные файлы
│   ├── python312.dll
│   ├── pyttsx3/
│   ├── speech_recognition/
│   └── ...
```

### Что нужно скопировать вместе с EXE?

```
📁 DotaCoach/
  ├── DotaCoach.exe       ← Программа
  ├── .env                ← Конфиг с API ключом!
  ├── _internal/          ← Автоматически копируется
  └── (всё остальное опционально)
```

### Файл `.env`

Скопируй `.env.example` в `.env` и заполни:

```ini
# .env
QWEN_API_KEY=your_actual_key_from_alibaba          # ← ВАЖНО!
QWEN_MODEL=qwen-max
DOTA2_PROCESS_NAME=dota2.exe
LANGUAGE=ru_RU
```

---

## ⚠️ ПРОБЛЕМЫ И РЕШЕНИЯ

### "Python не найден"
- Установи Python с https://www.python.org/
- Убедись, что отметил "Add Python to PATH"
- Перезагрузи компьютер

### "PyInstaller не установлен"
```bash
pip install PyInstaller==6.1.0
```

### EXE запускается, но нет звука
- Проверь наличие микрофона в ОС
- Проверь громкость
- Может, браузер/Discord/VLC блокирует доступ?

### "No module named 'speech_recognition'"
Это нормально - зависимости встроены в EXE.
Если ошибка всё равно есть, переустанови:

```bash
pip install --force-reinstall SpeechRecognition==3.10.0
```

### Большой размер EXE (~200-300 MB)
Это нормально! В EXE встроены:
- Python интерпретатор
- Все зависимости
- Библиотеки для речи
- И т.д.

Если нужен меньший размер, используй `--onedir` в PyInstaller.

---

## 🚀 РАСПРОСТРАНЕНИЕ

### ZIP архив для пользователей

```
1. Создай папку DotaCoach
2. Скопируй туда:
   - DotaCoach.exe
   - .env (с QWEN_API_KEY!)
   - README.txt с инструкциями
3. Архивируй: DotaCoach.zip
4. Распространяй!
```

### GitHub Releases

```bash
# Создай релиз на GitHub с:
# - DotaCoach.exe
# - .env.example
# - README.md с инструкциями
```

---

## 📊 ХАРАКТЕРИСТИКИ EXE

| Показатель | Значение |
|-----------|----------|
| Размер | ~200-300 MB |
| ОС | Windows 7+ |
| Требования | ~512 MB RAM |
| Python версия | 3.8+ (встроен) |
| Зависимости | Весь Python встроен |

---

## 🎓 ПРОДВИНУТЫЕ ОПЦИИ

### Использование UPX для компрессии

```bash
# Установи UPX: https://upx.github.io/
# Потом добавь флаг:

pyinstaller --onefile --upx-dir="C:\path\to\upx" main.py
```

### Скрытая консоль (без чёрного окна)

```bash
# Замени --console на --windowed
pyinstaller --onefile --windowed main.py
```

### Кастомная иконка

```bash
# Положи icon.ico в папку проекта
# Потом используй:
pyinstaller --icon=icon.ico main.py
```

---

## 📞 ПОМОЩЬ

Если что-то не работает:

1. Проверь логи - там обычно написана причина
2. Убедись, что Python установлен правильно (`python --version`)
3. Пересоздай виртуальное окружение
4. Открой Issue на GitHub

---

**Удачи! 🎮**
