# 🪟 ТОЛЬКО ДЛЯ WINDOWS

## Если ты видишь ошибку "Python не найден"

### ❌ ОШИБКА:
```
❌ Python не найден!

Установи Python с https://www.python.org/
⭐ Не забудь отметить "Add Python to PATH"
```

### ✅ РЕШЕНИЕ:

#### Шаг 1: Запусти помощник установки
```
Двойной клик на: install_python.bat
```

Скрипт автоматически откроет сайт Python.

#### Шаг 2: Скачай Python
```
На сайте нажми большую кнопку:
"Download Python 3.12"
```

#### Шаг 3: Запусти установщик
```
Двойной клик на скачанный файл python-3.12.*.exe
```

#### Шаг 4: ⭐ САМОЕ ВАЖНОЕ ⭐
```
В первом окне установщика ОТМЕТЬ ГАЛОЧКУ:

☑ Add Python 3.12 to PATH  ← ВОТ ЭТА!

Затем нажми "Install Now"
```

**Если этого не сделать - Python потом не будет работать!**

#### Шаг 5: Перезагрузи компьютер
```
После установки ОБЯЗАТЕЛЬНО перезагрузи ПК!
```

#### Шаг 6: Проверь что Python установлен

Открой **Командную строку** (CMD):
```
Нажми: Windows + R
Напиши: cmd
Нажми: Enter

Теперь напиши: python --version
```

Должно быть:
```
Python 3.12.0  ← Если видишь это - отлично!
```

#### Шаг 7: Теперь собирай EXE
```
Двойной клик на: build_windows_safe.bat
```

---

## 📊 СКРИПТЫ ДЛЯ WINDOWS

| Скрипт | Что делает |
|--------|-----------|
| `install_python.bat` | 🆕 Помощник установки Python |
| `build_windows_safe.bat` | Собрать EXE (использует минимум зависимостей) |
| `find_bad_package.bat` | 🔍 Найти проблемный пакет если ошибка |
| `build_windows_diagnostic.bat` | Полная диагностика системы |
| `build_windows_powershell.bat` | PowerShell версия с поиском Python |

---

## 🆘 Если ничего не работает

1. **Проверь что Python установлен:**
   ```
   Windows + R
   cmd
   python --version
   ```

2. **Если это не работает - переустанови Python:**
   - Открой "Панель управления" → Программы
   - Находи Python и удали его
   - Перезагрузись
   - Установи заново (не забудь "Add Python to PATH"!)
   - Перезагрузись еще раз

3. **Если всё равно не работает:**
   - Скопируй всё что написано на экране
   - Открой Issue на GitHub
   - Вставь скопированный текст

---

## 💡 БЫСТРЫЙ ЧЕКЛИСТ

- [ ] Установил Python 3.10+ с https://www.python.org/
- [ ] Отметил галочку "Add Python to PATH" при установке
- [ ] Перезагрузил компьютер
- [ ] Проверил `python --version` в CMD
- [ ] Запустил `build_windows_safe.bat`
- [ ] Дождался завершения сборки (3-5 минут)
- [ ] Нашел `dist/DotaCoach.exe` ✅

---

**Если всё сделаешь правильно - всё будет работать! 🚀**
